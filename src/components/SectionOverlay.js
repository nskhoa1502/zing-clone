import React from "react";
import icons from "../utils/icons";

const { BsFillPlayFill, BsDot, AiOutlineSearch } = icons;

const SectionOverlay = ({}) => {
  return (
    <div className="absolute top-0 bottom-0 left-0 right-0 bg-overlay-30 rounded-md"></div>
  );
};

export default SectionOverlay;
